export * from "./components/circle-indicator";
// Setup:

// 1) npm install --save-dev react react-dom
// (as dev dependencies so they are not auto installed on user application)(avoid collisions with users v react/etc)

//2) List as peer dependencies to ensure they are installed

//3) Add storybook for live preview when building component:
//  npx sb init

// 4) Run for preview:
// npm run storybook

// 5) create component with export

// 6) export component from index.js entrypoint (this file)

//7) Create Component.stories.js file for building a live preview
